// import testData from "../../db.json";

// export const initialState = { ...testData };
export const initialState = {
  pastimeFilters: [],
  screen: 0,
  formData: {},
  minAge: 18,
  maxAge: 100,
};
